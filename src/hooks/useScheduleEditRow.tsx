import IPair from "../models/interfaces/IPair";
import {useScheduleRow} from "./useScheduleRow";


export const useScheduleEditRow = (pair: IPair, filterType, key: number) => {

    const row = useScheduleRow(pair, filterType, key);





}