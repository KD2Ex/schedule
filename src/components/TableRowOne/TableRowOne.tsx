import React, {FC} from 'react';
import {TableRowProps} from "@mui/material";

interface TableRowOneProps {



}

const TableRowOne: FC<TableRowProps> = ({cells}) => {
	return (
		<div>

		</div>
	);
};

export default TableRowOne;